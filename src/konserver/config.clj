(ns konserver.config)

(def *root* "http://raek.ath.cx:8080/")

(def *server-name* "Konserver Test Server (Alpha)")

(def *data-directory* "data")

(def *groups*
     #{{:id :test, :name "Test Group"}})
